import 'package:flutter/material.dart';

class PayFail extends StatefulWidget {
  const PayFail({super.key});

  @override
  State<PayFail> createState() => _PayFailState();
}

class _PayFailState extends State<PayFail> {
  @override
  Widget build(BuildContext context) {
    return const Text('Thất Bại');
  }
}
